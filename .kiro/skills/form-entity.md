When given a backend entity file via #file, generate: 
(1) a controlled React form component with field validation matching entity constraints, 
(2) a list component that fetches and renders items, 
(3) an API client function for each endpoint. Use the entity field types directly — do not re-define them in the frontend.